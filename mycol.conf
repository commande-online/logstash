input {
        file {
                type => "myCOL"
                path => "PATH"
        }
}
filter{

        grok {
                match => { "message" => "\[%{DATESTAMP_RFC2822:timestamp}\]\[%{LOGLEVEL:log}\] %{GREEDYDATA:data}"
                }
        }
}
output {
        http {
                url => "http://IP/gelf"
                http_method => "post"
        }
}